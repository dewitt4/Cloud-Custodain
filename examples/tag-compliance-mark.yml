ec2:
  filters:
    - filter: instance-state-name
      type: ec2
      value: running
    - filter: tag:CMDBEnvironment
      type: ec2
      state: absent
    - filter: tag:ASV
      type: ec2
      state: absent
    - filter: tag:OwnerEID
      type: ec2
      state: absent
    - filter: tag:OwnerEmail
      type: ec2
      state: absent
  actions:
    - type: mark
      tag: maid_status
