filters:
  - filter: instance-state-name
    type: ec2
    value: running
  - filter: tag:CMDBEnvironment
    type: ec2
    state: absent
  - filter: tag:ASV
    type: ec2
    state: absent
actions:
  - mark
